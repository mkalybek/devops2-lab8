apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
  - pairs:
      project: myapp
    includeSelectors: true

resources:
  - deployment-db.yaml
  - deployment-app.yaml
  - service-db.yaml
  - service-app.yaml
  - ingress.yaml
